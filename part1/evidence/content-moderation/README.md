# Evidence: BUG-3 - no content moderation on "Modify Script with AI"

The AI rewrite produced profanity and self-harm-themed content with no filtering,
warning, or refusal.

> **Screenshot withheld from this public repository.** The evidence image
> (`rewrite-unmoderated.png`) necessarily contains the flagged content, so it is
> gitignored and shared only with the private submission - it is not embedded
> here to avoid publishing that content. The screenshot shows the editor's script
> panel populated by the unmoderated rewrite.

One screenshot is sufficient; there is no need to generate further or more severe
content to prove the point.
